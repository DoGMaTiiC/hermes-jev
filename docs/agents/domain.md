# Domain docs

Single-context layout: `CONTEXT.md` at the repo root (create when the first domain term needs
recording) and ADRs under `docs/adr/` (`NNNN-slug.md`).

Consumers: skills read `CONTEXT.md` for vocabulary and respect ADRs in the area they touch.
