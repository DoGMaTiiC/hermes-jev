# Triage labels

Label strings equal their role names (defaults):

- `needs-triage` — new, not yet classified
- `needs-info` — waiting on the reporter
- `ready-for-agent` — agent-grabbable
- `ready-for-human` — needs a human
- `wontfix` — explicitly not doing
